<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.careLink.Member.mapper.MemberMapper">

    <!--회원가입 -->
    <insert id="save">
        <selectKey keyProperty="memberId" order="BEFORE" resultType="int">
            select member_seq.nextval from dual
        </selectKey>

        INSERT INTO MEMBER(MEMBER_ID, ID, PASSWORD, MEMBER_NAME,  MEMBER_EMAIL, MEMBER_TEL, MEMBER_ADDRESS, ROLE, WITHDRAWAL, AGREE)
        VALUES (#{memberId}, #{id}, #{password}, #{memberName}, #{memberEmail}, #{memberTel}, #{memberAddress}, #{role}, #{withdrawal}, #{agree})
    </insert>

    <!-- 회원정보 -->
    <select id="info" parameterType="String" resultType="java.util.Optional">
        SELECT * FROM MEMBER WHERE ID = #{id}
    </select>

</mapper>

